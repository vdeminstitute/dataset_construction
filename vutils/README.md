## vutils

Internal package for organizing Rcpp/R functions used in the V-Dem DS
construction process.

#### Installation

`vutils` is available from the [V-Dem CRAN repository](http://dev.v-dem.net/R). The versioning scheme is based on the number of commits since the last tag (*i.e.*, DS release).

```R
install.packages("vutils")
```

